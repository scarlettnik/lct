stages:
  - build

npm-build:
  stage: build
  image: node:20-bookworm-slim
  variables:
    NPM_CONFIG_CACHE: /cache/npm
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist
